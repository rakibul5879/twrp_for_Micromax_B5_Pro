name: Manual Dispatch Helper

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Notify
        run: |
          echo "Manual dispatch detected. To run the full build, trigger 'Build TWRP (workflow_dispatch)' or provide a manifest URL."
